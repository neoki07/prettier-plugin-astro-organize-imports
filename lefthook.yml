pre-commit:
  commands:
    quality:
      run: npm run format:check && npm run lint && npm run type-check && npm test

pre-push:
  commands:
    quality:
      run: npm run format:check && npm run lint && npm run type-check && npm test
